// SQL script to create the nasa_apod table in PostgreSQL
CREATE TABLE public.nasa_apod
(
	id BIGSERIAL PRIMARY KEY,
	title varchar(255),
	url TEXT,
	explanation TEXT,
	hdurl TEXT,
	media_type varchar(255),
	service_version varchar(255),
	date DATE,
	copyright varchar(255)
);